#include<iostream>
using namespace std;
#include<vector>
int main(){
    vector<int>v1={1,2,3};
    vector<int>v2={4,5,6};
    vector<int>::iterator it;
    v1.swap(v2);
    for(it=v1.begin();it!=v1.end();it++){
        cout<<*it<<" ";
    }
}
